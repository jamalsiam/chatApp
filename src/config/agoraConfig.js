// Agora Configuration
// Get your App ID from: https://console.agora.io/

const AgoraConfig = {
  // Replace with your Agora App ID
  appId: 'YOUR_AGORA_APP_ID',

  // Token server URL (optional, for production)
  // If null, will use App ID only (works for testing)
  tokenServerUrl: null, // 'https://your-token-server.com/rtc-token'
};

export default AgoraConfig;
